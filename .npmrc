# Use a conventional commit message for version bumps
message = "chore(release): %s"

# Do not prefix the git tag with 'v'
tag-version-prefix = ""

# Sign the git tag if a GPG key is configured
sign-git-tag = true
